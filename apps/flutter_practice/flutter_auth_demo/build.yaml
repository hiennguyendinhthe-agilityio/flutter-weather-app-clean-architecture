targets:
  $default:
    builders:
      injectable_generator:injectable_builder:
        enabled: true
        generate_for:
          - lib/**/*.dart
      json_serializable:
        enabled: true
        generate_for:
          - lib/data/models/*.dart